# TechLink Navigator

TechLink Navigator 是一个面向开发者与技术爱好者的高质量外链资源聚合与导航系统。项目定位于解决技术信息碎片化、优质资源分散、检索效率低下等问题，通过人工筛选与自动化巡检相结合的方式，持续维护一份高可用性的技术资源索引。目标用户包括一线研发人员、技术团队负责人、开源贡献者以及各类技术内容消费者。通过结构化分类、状态监控与快速跳转能力，本系统显著降低技术调研与日常开发中的信息获取成本。

## 功能概览

- **智能分类索引** 按照技术栈、应用场景、服务类型等维度对链接进行多级标签管理，支持自定义分类规则，便于团队内部统一资源入口。

- **可用性主动监测** 内置周期性 HTTP 状态检查机制，对收录的每一个外链资源进行可用性探测，自动标记异常链接并生成告警日志，确保索引列表的鲜活度。

- **快速检索与过滤** 提供基于关键词、分类标签、添加时间、状态等多条件组合检索，响应时间低于 200 毫秒，支持模糊匹配与精准匹配两种模式。

- **自定义分类体系** 允许管理员动态增删改分类节点，支持树形结构嵌套，可适应不同业务场景下的资源组织需求，分类变更自动关联已有链接。

- **访问统计与热度分析** 记录每个外链的点击次数、最近访问时间、来源 IP 地域分布等基础指标，提供热度排行榜与趋势图，辅助判断资源价值。

- **批量导入与导出** 支持通过 CSV 或 JSON 格式批量导入链接数据，亦可按分类或全量导出为结构化文件，便于系统间迁移或备份。

- **链接备注与版本记录** 每个链接可附加多行备注信息，记录添加原因、推荐人、使用注意事项等，同时自动保存每次修改的历史版本，支持回滚。

- **响应式管理面板** 提供移动端适配的管理界面，核心操作流程针对触屏优化，便于在移动设备上快速审批新增链接或处理异常告警。

## 应用场景

1. **技术团队内部知识库建设** 技术负责人可使用 TechLink Navigator 统一整理团队常用开发文档、API 参考、组件库、监控面板等外部资源链接，替代零散的浏览器书签，新成员入职时可一键获取所有必备工具入口。

2. **开源项目推荐文档维护** 开源项目维护者将项目依赖的第三方服务、社区论坛、CI/CD 工具、代码质量平台等链接集中管理，并在项目 README 中引用本系统的公开视图，避免多处重复维护链接列表。

3. **技术博客与资讯聚合** 技术内容创作者利用本系统按周或按月整理高质量技术文章、播客、视频教程、电子书等学习资源，生成公开分享页，供读者快速访问，同时通过访问统计了解读者兴趣偏好。

4. **企业内网服务导航** 企业内部部署 TechLink Navigator，整合 GitLab、JIRA、Confluence、SonarQube、Nexus、Harbor 等各类内部平台入口，配合可用性监测功能提前预警服务不可用状态，减少故障排查时间。

5. **技术社区资源共建** 技术社区运营方可开放部分分类的编辑权限，引导社区成员共同维护一份高质量的外链索引，通过审核机制控制质量，形成社区专属的资源沉淀池。

## 快速开始

以下步骤基于 Linux / macOS 环境，使用 Node.js 20 LTS 版本。Windows 用户建议使用 WSL2 或 Git Bash。

```bash
# 克隆项目仓库
git clone https://github.com/techlink-navigator/navigator.git
cd navigator

# 安装项目依赖（使用 npm）
npm install

# 复制默认配置文件并修改数据库连接等参数
cp .env.example .env

# 初始化数据库表结构与基础分类数据
npm run db:init

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

启动成功后，访问 `http://localhost:3000` 即可进入管理面板。首次登录默认管理员账户为 `admin@techlink.local`，密码为 `admin123`，请务必在首次登录后修改密码。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 20.x LTS 或更高 | 运行时环境，推荐使用 nvm 管理版本 |
| npm | 10.x 或更高 | 包管理器，随 Node.js 一同安装 |
| PostgreSQL | 15.x 或更高 | 主数据库，存储链接、分类、用户、统计等数据 |
| Redis | 7.x 或更高 | 缓存与会话存储，用于提升检索性能与分布式部署 |
| PM2 | 5.x 或更高 | 生产环境进程管理工具（可选，但强烈推荐） |
| Nginx | 1.24.x 或更高 | 反向代理与静态资源服务（生产环境推荐） |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/user-guide/` | 如何注册、登录、添加链接、创建分类、配置个人偏好？ |
| 管理员手册 | `/docs/admin-guide/` | 如何管理用户权限、审核链接、配置监测策略、查看系统日志？ |
| 开发者指南 | `/docs/developer-guide/` | 如何二次开发、扩展插件、调试本地环境、编写单元测试？ |
| API 参考 | `/docs/api-reference/` | 所有 RESTful API 的端点定义、请求参数、响应格式与错误码说明？ |
| 部署运维 | `/docs/deployment/` | 如何在生产环境部署、配置 SSL、设置备份策略、进行性能调优？ |
| 架构设计 | `/docs/architecture/` | 系统整体架构图、数据流向、模块职责、关键技术选型理由？ |

## 资源列表

本系统收录的外部资源链接按服务类型划分如下。所有链接均经过初始可用性验证，后续由系统自动持续监测。

**社交与交友类服务**

- <code>fujinderenyueai.com.cn</code>
- <code>shenyeshangmen.com.cn</code>
- <code>jiaoyouyiyeqing.com.cn</code>
- <code>moliaoyue.com.cn</code>
- <code>tongchengyue.com.cn</code>
- <code>yueaiwang.com.cn</code>

## 项目结构

```
navigator/
├── src/                                # 源代码主目录
│   ├── api/                            # RESTful API 路由与控制器
│   │   ├── v1/                         # API 版本 v1 实现
│   │   │   ├── links.js                # 链接资源 CRUD 操作
│   │   │   ├── categories.js           # 分类管理接口
│   │   │   ├── users.js                # 用户认证与个人信息
│   │   │   └── stats.js                # 访问统计与热度数据
│   │   └── middleware/                 # 通用中间件（鉴权、限流、日志）
│   ├── core/                           # 核心业务逻辑层
│   │   ├── monitor/                    # 链接可用性监测引擎
│   │   │   ├── checker.js              # HTTP 状态检查实现
│   │   │   └── scheduler.js            # 定时任务调度配置
│   │   ├── search/                     # 检索与过滤引擎
│   │   │   ├── indexer.js              # 全文索引构建
│   │   │   └── query.js                # 查询解析与执行
│   │   └── cache/                      # 缓存策略与 Redis 封装
│   ├── models/                         # 数据模型层（Sequelize / Prisma）
│   │   ├── Link.js                     # 链接表模型
│   │   ├── Category.js                 # 分类表模型
│   │   ├── User.js                     # 用户表模型
│   │   └── VisitLog.js                 # 访问日志表模型
│   ├── services/                       # 外部服务集成层
│   │   ├── email/                      # 邮件通知服务
│   │   ├── queue/                      # 消息队列（Bull）封装
│   │   └── webhook/                    # 出站 Webhook 分发
│   ├── web/                            # 前端管理面板（React + Vite）
│   │   ├── pages/                      # 页面级组件
│   │   ├── components/                 # 可复用 UI 组件
│   │   └── hooks/                      # 自定义 React Hooks
│   └── utils/                          # 通用工具函数
│       ├── validator.js                # 输入校验与清洗
│       ├── logger.js                   # 日志工具（Winston）
│       └── config.js                   # 配置加载与管理
├── tests/                              # 单元测试与集成测试
│   ├── unit/                           # 单元测试用例
│   └── integration/                    # 集成测试用例
├── scripts/                            # 运维与辅助脚本
│   ├── db-init.js                      # 数据库初始化脚本
│   ├── seed-data.js                    # 初始分类与测试数据填充
│   └── health-check.js                 # 手动触发全量健康检查
├── docs/                               # 项目文档（见上文导航）
├── logs/                               # 应用日志输出目录（生产环境）
├── .env.example                        # 环境变量配置模板
├── package.json                        # npm 依赖与脚本定义
├── ecosystem.config.js                 # PM2 进程配置
└── README.md                           # 本文件
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增分类建议、链接资源推荐、代码缺陷修复、文档完善以及功能提案。请遵循以下步骤：

1. 在 GitHub 仓库页面点击 `Fork` 按钮，将项目复制到您的个人账户下，然后执行 `git clone` 将仓库拉取到本地开发环境。

2. 在本地新建一个功能分支，分支名称使用 `feat/`、`fix/`、`docs/` 前缀，后接简要描述，例如 `feat/add-ai-category` 或 `fix/monitor-timeout`。

3. 完成代码或文档修改后，确保所有单元测试通过（执行 `npm test`），并且代码符合项目 ESLint 规则（执行 `npm run lint`）。新增功能需附带相应的测试用例。

4. 提交代码时使用清晰的 commit message，遵循 Conventional Commits 规范，例如 `feat(monitor): add retry mechanism for failed checks`。

5. 向主仓库的 `develop` 分支发起 Pull Request，在 PR 描述中详细说明修改内容、测试覆盖情况以及相关 Issue 编号。项目维护者将在 3 个工作日内进行 Code Review 并给出合入意见。

## 常见问题

**Q：系统能否支持 SQLite 作为数据库以简化本地测试？**

A：当前版本仅支持 PostgreSQL，因为部分查询使用了 PG 特有的 JSONB 和全文检索功能。若仅进行前端界面开发或文档编写，可以使用 `npm run dev:mock` 命令启动模拟数据模式，该模式不依赖真实数据库。完整功能测试仍需 PostgreSQL 环境。

**Q：可用性监测的频率和超时时间是如何配置的？**

A：监测频率和超时时间均在 `.env` 文件中通过环境变量配置。默认每 6 小时执行一次全量检查，单链接超时阈值为 5000 毫秒。对于特殊链接（如内网地址或需要较长响应时间的服务），可在管理面板中单独设置覆盖参数。监测结果会记录到 `monitor_logs` 表中，便于追溯历史状态变化。

**Q：如何将现有的浏览器书签批量导入系统？**

A：系统提供了 CSV 导入功能。您需要将书签导出为 HTML 格式（浏览器自带导出功能），然后使用项目 `scripts/convert-bookmark.js` 脚本将其转换为系统兼容的 CSV 格式。转换后，在管理面板的“批量导入”页面选择该 CSV 文件并提交，系统会自动解析并创建对应的分类与链接记录。请注意，导入前建议先预览分类映射关系，避免产生过多冗余分类。

## 许可证

MIT

> 外链数量: 6 | 生成时间: 2026-08-21 18:43:36
